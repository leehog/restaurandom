import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  wrapper: {
    position: 'relative',
    flexGrow: 1,
    zIndex: 0
  },
  gradientWrapper: {
    position: 'absolute',
    width: '100%'
  }
})